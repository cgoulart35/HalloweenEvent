APScheduler==3.10.4
bcrypt==4.1.2
debugpy==1.6.7
firebase-admin==7.4.0
Flask==3.1.3
Flask-Cors==6.0.2
Flask-RESTful==0.3.10
Flask-Toastr==0.5.8
numpy==2.4.6
opencv-python==4.13.0.92
# qrcode's recommended image backend (was previously present transitively via the
# torch/ultralytics stack). Required so qrcode.make(...).save(path) keeps working
# for the per-user QR codes in queries.py.
Pillow==12.2.0
qrcode==7.4.2
# Pinned to clear PYSEC-2023-74 / CVE-2024-35195 / CVE-2024-47081 / CVE-2026-25645.
# With Pyrebase4 replaced by firebase-admin, urllib3 is no longer capped at <2 and
# now resolves to a patched 2.x, clearing its advisories as well.
requests==2.34.2
