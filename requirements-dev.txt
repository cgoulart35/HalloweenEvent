# Dev/test-only dependencies. Not installed into the runtime image by the
# Dockerfiles (which only install requirements.txt) -- installed ad hoc when
# running the test/audit suite, e.g.:
#   pip install -r requirements-dev.txt && pytest && pip-audit
# Tests use the built-in `monkeypatch` fixture, so pytest-mock is not needed.
pytest==9.0.3
pip-audit==2.10.0
